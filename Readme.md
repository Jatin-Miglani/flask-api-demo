# Flask API Demo

This is a minimal Flask API used for testing web deployment platforms like Render or Railway.

## Endpoints

- `/` → Returns a welcome message
- `/status` → Returns API status

## How to run locally

```bash
pip install -r requirements.txt
python app.py